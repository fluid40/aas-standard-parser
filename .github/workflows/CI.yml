name: CI

on:
  push:
    branches:
      - '**'

permissions:
  contents: write
  pull-requests: read

jobs:
  Detect_Project_Changes:
    name: Check for new version
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.check.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check if pyproject.toml changed
        id: check
        run: |
          if git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep -q '^pyproject\.toml$'; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

  Run_Tests:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest -v tests/*.py

  Publish_PyPI_Package:
    name: Publish PyPI Package
    if: github.ref == 'refs/heads/main' && needs.Detect_Project_Changes.outputs.changed == 'true'
    runs-on: ubuntu-latest
    needs: [Detect_Project_Changes, Run_Tests]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Upload package as artifact
        uses: actions/upload-artifact@v4
        with:
          name: pypi-dist
          path: dist/*

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: twine upload dist/*

  Github_Release:
    name: Create GitHub Release
    if: github.ref == 'refs/heads/main' && needs.Detect_Project_Changes.outputs.changed == 'true'
    runs-on: ubuntu-latest
    needs: Publish_PyPI_Package

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: pypi-dist
          path: dist

      - name: Extract version from pyproject.toml
        id: get_version
        run: |
          VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted VERSION=$VERSION"

      - name: Generate Changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v4
        with:
          configuration: .github/changelog-config.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Git tag if it doesn't exist
        run: |
          VERSION_TAG="v${{ steps.get_version.outputs.version }}"
          if git rev-parse "$VERSION_TAG" >/dev/null 2>&1; then
            echo "Tag $VERSION_TAG already exists."
          else
            echo "Creating tag $VERSION_TAG"
            git tag "$VERSION_TAG"
            git push origin "$VERSION_TAG"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: 🚀 Release v${{ steps.get_version.outputs.version }}
          generate_release_notes: true
          body: |
            ## 📝 Release Notes

            **Version:** v${{ steps.get_version.outputs.version }}
            **Commit:** `${{ steps.get_version.outputs.hash }}`

            ### 🧩 Build Information
            - Pypi package: `aas-standard-parser ${{ steps.get_version.outputs.version }}`
            - `pip install aas-standard-parser`

            ### 🔗 Links
            - [Compare changes](https://github.com/${{ github.repository }}/compare/last...v${{ steps.get_version.outputs.version }})
            - [Full commit log](https://github.com/${{ github.repository }}/commits/main)
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

